# Create a key at https://console.anthropic.com/settings/keys
# Copy this file to .env and paste your key in. .env is gitignored.
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: pin requests to a specific Console workspace key instead of the
# default workspace by using that workspace's key above. Nothing else needed.
